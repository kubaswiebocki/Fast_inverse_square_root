# Biblioteka
#####################################################################
import struct
#####################################################################

# Otwarcie pliku
#####################################################################
e = 0
def float_to_bin(num):
    bits, = struct.unpack('!I', struct.pack('!f', num))
    return "{:032b}".format(bits)

f = open("InputData.mem", 'w')
f.write('')
f.close()
f = open("InputData.mem", 'a')
#####################################################################

# Zapis wartości do pliku
#####################################################################
for i in range(1000):
  e = e + 0.01
  x = str(float_to_bin(e))
  f.write(x + '\n')

#####################################################################
f.close()